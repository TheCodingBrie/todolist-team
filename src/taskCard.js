
urgent tag if < 1 day

task.title

background = importance color

complete (grey if progress < 100)

modify (rename) popup or extra input

delete

progress bar

deadline

.map((subTaskCard)=> <subTaskCard />)

hidden : redo / trash